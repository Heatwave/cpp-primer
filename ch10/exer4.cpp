#include <iostream>
#include <numeric>
#include <vector>
#include "ch10.h"

using std::vector;
using std::accumulate;
using std::cout;
using std::endl;

void exer4()
{
	vector<double> ivec{ 1.3, 2.6 };
	//auto sum = accumulate(ivec.cbegin(), ivec.cend(), 0);
	//cout << sum << endl;
}
