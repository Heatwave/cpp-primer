#include <iostream>
#include <vector>

using std::cout;
using std::endl;
using std::vector;

void exercise19()
{
	int *ptr = NULL;
	vector<int> vec{ 1,2,3,4,5 };
	int ival = 1;

	ptr != 0 && *ptr++;
	ival++ && ival;
	vec[ival] <= vec[ival + 1];
}
